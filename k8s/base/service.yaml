apiVersion: v1
kind: Service
metadata:
  name: splittrack-service
  labels:
    app.kubernetes.io/name: splittrack
    app.kubernetes.io/component: web-api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: splittrack
